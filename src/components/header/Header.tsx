import React from "react";
import "./Header.css";

export const Header = () => {
  return (
    <div className={"Header"}>
      <div className={"Header-text"}>Test</div>
    </div>
  );
};

const classNames = {
  root: {
    backgroundColor: "blue",
  },
};
